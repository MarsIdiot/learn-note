1.while循环
	while（循环条件）{
		循环操作
	}
	//满足条件才执行（先判断再执行）
2.do-while循环
	do{
		循环操作
	}while(循环条件);
	//至少执行一次（即先执行再判断）
3.for循环
	for( 参数初始化; 条件判断 ;更新循环变量  ){
		循环操作；
	}	
	eg:
	for(int i=0;i<10;i++){
		i+=2;	
	} 