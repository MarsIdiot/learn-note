##  第十九周总结2018.11.30

## 大致总结

### （一)本周学习内容：

   	本周继续开发电子签章系统。主要进一步完善用户服务统一编排层，开发合同下载验签等合同相关服务编排层，然后在原来的基础上进行对日志和异常的规范统一完善。

​	另外，在参与项目中，看到自己知识的不足。如：签名验签的使用、字段相关设计(not null、null、空值“、defalut、等如何正确使用，varchar(n)中n的含义等)

### （二）问题与反思

​	1、在书写代码时，一个方法不宜太长，可以抽取方法、优化结构。

​	2、关于日志和异常：在前期编写的时候出现的问题包括有：有些地方重复地校验参数、日志输出太笼统该详细到错误点、异常抛出太详细该笼统(参数错误、用户创建等模块性错误)。

​	3、bean重复：

​	问题：在书写service时，接口service类名和serviceImpl注解重复。在启动时会报错bean冲突，接口名重复。
​	原因：由于在引用时是使用接口引入，所以如果接口名相同会造成spring容器不知道去找哪个实现类。
​	解决：1）、@service("xxxNew")
​		  2）、如果不修改接口名，须在引入时说明：@Resource(name="xxxNew")

​	4、测试相关：

​		1）测试修改代码时，配置文件等有时候不会自动编译，常出现文件找不到、qlStatement不存在等错误。开启热部署或及时手动编译。	

​		2）在进行junit test时，@IbatisAliasConfig(alias = "xxx")会失效，如果要使用别名，需单独配置。如：<sqlMap><typeAlias alias="xxx" type="com.xxx.xxx.user.beans.xxx" /></sqlMap>

​		3）在引入Bean存在错误时，

​		如： 
​		@Autowired 
​		private UserDao userDao;
​		最好写成userDao，否则可能存在导入错误。

### （三）下周计划

​    	参与项目的开发。



